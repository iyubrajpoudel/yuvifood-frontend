/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'yuvifood-backend.onrender.com'
        ]
    }
}

module.exports = nextConfig
